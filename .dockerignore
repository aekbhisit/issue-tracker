# Very minimal exclusions - only exclude what's absolutely necessary
# This ensures apps/ directory is always included

.git
.gitignore
.vscode
.DS_Store
*.log
.env*
storage
tmp
coverage
.nyc_output

# Exclude node_modules but be specific to avoid excluding parent dirs
node_modules
.pnpm-store
.pnpm

# Exclude build outputs (but source files in apps/ are kept)
dist
build
.next
.turbo

# CRITICAL: apps/ directory and its source files MUST be included
# Do not add patterns that might exclude apps/
