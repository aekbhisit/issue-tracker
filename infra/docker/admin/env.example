# ===========================================
# Admin Dashboard Environment Variables
# ===========================================
# Copy this file to apps/admin/.env.local and fill in your values
# See docs/development/environment-variables.md for complete documentation
#
# NOTE: Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# and must be set at build time. They are baked into the Next.js bundle.

# ===========================================
# Public Environment Variables (accessible in browser)
# ===========================================
# Base API URL (used for health checks and version endpoint)
# Development example:
NEXT_PUBLIC_API_URL=http://localhost:4501
# Production example:
# NEXT_PUBLIC_API_URL=https://api.example.com

# Admin API endpoint URL
# Development example:
NEXT_PUBLIC_API_ADMIN_URL=http://localhost:4501/api/admin/v1
# Production example:
# NEXT_PUBLIC_API_ADMIN_URL=https://api.example.com/api/admin/v1

# Application name displayed in UI
NEXT_PUBLIC_APP_NAME=Issue Collector Admin

# ===========================================
# Private Environment Variables (server-side only)
# ===========================================
# Server-side secret key (if needed for admin-specific operations)
API_SECRET_KEY=your-secret-key

