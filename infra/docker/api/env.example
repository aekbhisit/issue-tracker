# ===========================================
# API Server Environment Variables
# ===========================================
# Copy this file to apps/api/.env.local and fill in your values
# See docs/development/environment-variables.md for complete documentation

# ===========================================
# Node Environment
# ===========================================
# Options: development, staging, production, test
NODE_ENV=development

# ===========================================
# Server Configuration
# ===========================================
# Port number for the API server
API_PORT=4501
# Hostname for the API server
API_HOST=localhost

# ===========================================
# Database Configuration
# ===========================================
# REQUIRED: PostgreSQL connection string (must be a valid URL)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/mydb
# Database type: postgresql or mysql
DATABASE_TYPE=postgresql

# ===========================================
# Security Configuration
# ===========================================
# REQUIRED: Secret key for JWT token signing (minimum 32 characters)
# Generate a strong random secret for production!
JWT_SECRET=your-secret-key-change-in-production-minimum-32-characters-long
# JWT token expiration time (e.g., 7d, 24h, 1h)
JWT_EXPIRES_IN=7d
# Number of bcrypt rounds for password hashing (10-15)
BCRYPT_ROUNDS=10

# ===========================================
# CORS Configuration
# ===========================================
# Comma-separated list of allowed CORS origins, or * for all
# Development example:
ALLOWED_ORIGINS=http://localhost:4502,http://localhost:4503
# Production example:
# ALLOWED_ORIGINS=https://admin.example.com,https://app.example.com

# ===========================================
# Storage Configuration
# ===========================================
# Storage type: local or s3
STORAGE_TYPE=local
# Local storage paths
STORAGE_PATH=./storage/uploads
STORAGE_PUBLIC_PATH=./storage/public
STORAGE_TEMP_PATH=./storage/uploads/temp
# Base URL for local storage (optional)
LOCAL_STORAGE_BASE_URL=http://localhost:4501/uploads

# ===========================================
# AWS S3 Configuration (Required if STORAGE_TYPE=s3)
# ===========================================
# Uncomment and fill in if using S3 storage:
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_S3_BUCKET=your-bucket-name
# AWS_S3_REGION=ap-southeast-1
# CDN_DOMAIN=https://cdn.example.com

# ===========================================
# Upload Limits
# ===========================================
# Maximum file size in bytes (default: 10MB = 10485760)
MAX_FILE_SIZE=10485760
# Comma-separated list of allowed image types
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif,webp
# Comma-separated list of allowed document types
ALLOWED_DOCUMENT_TYPES=pdf,doc,docx

# ===========================================
# Cleanup Configuration
# ===========================================
# Hours after which temporary files are cleaned up
TEMP_FILE_CLEANUP_HOURS=24

# ===========================================
# Redis Configuration (Optional)
# ===========================================
# Redis connection URL for caching/queues (optional)
# REDIS_URL=redis://localhost:6379

