# Environment Variables Example
# คัดลอกไฟล์นี้เป็น .env และแก้ไขค่าตามความต้องการ

# ===========================================
# Development vs Production Usage
# ===========================================
# Development: ใช้ docker-compose.yml (default)
# Database Only: ใช้ docker-compose.dev-db-only.yml
# Staging: ใช้ docker-compose.staging.yml
# Production: ใช้ docker-compose.prod.yml

# ===========================================
# Docker Registry Configuration (for staging/production)
# ===========================================
DOCKER_REGISTRY=your-registry.com
DOCKER_USERNAME=your-docker-username
DOCKER_PASSWORD=your-docker-password

# ===========================================
# Database Configuration
# ===========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=mydb

# ===========================================
# Application Configuration
# ===========================================
NODE_ENV=development
JWT_SECRET=your-jwt-secret-key-here

# ===========================================
# API URLs Configuration
# ===========================================
# Development URLs
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_API_ADMIN_URL=http://localhost:3000/api/admin/v1

# Production URLs (uncomment for production)
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# NEXT_PUBLIC_API_ADMIN_URL=https://api.yourdomain.com/api/admin/v1

# ===========================================
# CORS Configuration
# ===========================================
# Development
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:3002

# Production (uncomment and modify for production)
# ALLOWED_ORIGINS=https://admin.yourdomain.com,https://yourdomain.com

# ===========================================
# Storage Configuration
# ===========================================
STORAGE_TYPE=local
STORAGE_PATH=./storage/uploads

# Production Storage (uncomment for production)
# STORAGE_TYPE=s3
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_S3_BUCKET=your-bucket-name
# AWS_S3_REGION=ap-southeast-1

# ===========================================
# Docker Images (for staging/production)
# ===========================================
API_IMAGE=your-registry.com/api:latest
ADMIN_IMAGE=your-registry.com/admin:latest
FRONTEND_IMAGE=your-registry.com/frontend:latest

# ===========================================
# Build Configuration
# ===========================================
BUILD_NUMBER=1
GIT_COMMIT_SHORT=dev
IMAGE_TAG=latest

# ===========================================
# Jenkins Configuration
# ===========================================
JENKINS_PORT=8080
JENKINS_HOME=C:\Jenkins

# ===========================================
# SSL Configuration (for production)
# ===========================================
SSL_CERT_PATH=./ssl/cert.pem
SSL_KEY_PATH=./ssl/key.pem

# ===========================================
# Backup Configuration
# ===========================================
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30

# ===========================================
# Development Hot Reload Configuration
# ===========================================
# These are automatically set in docker-compose.override.yml
# CHOKIDAR_USEPOLLING=true
# WATCHPACK_POLLING=true
