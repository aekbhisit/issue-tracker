# Site configuration for issue.haahii.com
# This file should be placed in /etc/nginx/sites-available/issue.haahii.com.conf
# and symlinked to /etc/nginx/sites-enabled/issue.haahii.com.conf
#
# Usage:
#   sudo cp infra/nginx/issue.haahii.com.conf /etc/nginx/sites-available/issue.haahii.com.conf
#   sudo ln -s /etc/nginx/sites-available/issue.haahii.com.conf /etc/nginx/sites-enabled/issue.haahii.com.conf
#   sudo nginx -t
#   sudo systemctl reload nginx

# HTTP server - redirects to HTTPS in production
server {
    listen 80;
    listen [::]:80;
    server_name issue.haahii.com;
    
    # Redirect HTTP to HTTPS (uncomment for production with SSL)
    # return 301 https://$server_name$request_uri;
    
    # For localhost testing without SSL, comment out the redirect above

    client_max_body_size 50M;
    client_body_timeout 60s;
    client_header_timeout 60s;

    # API endpoints
    # Proxies directly to API backend on localhost:3410
    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;
        
        proxy_pass http://localhost:3410;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Upload endpoints (rate limited, larger body size)
    location /api/admin/v1/upload/ {
        limit_req zone=upload_limit burst=5 nodelay;
        client_max_body_size 100M;
        
        proxy_pass http://localhost:3410;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Longer timeouts for file uploads
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # Static files (uploads) - served directly by nginx
    location /uploads/ {
        alias /usr/share/nginx/html/storage/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin *;
        
        # Security headers
        add_header X-Content-Type-Options "nosniff";
        add_header X-Frame-Options "SAMEORIGIN";
    }

    # Public static files
    location /public/ {
        alias /usr/share/nginx/html/storage/public/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin *;
    }

        # Admin Next.js static files with caching
    # With basePath: '/admin', Next.js expects requests with /admin prefix
    # Preserve full path so Next.js can handle basePath routing internally
    location /admin/_next/static/ {
        proxy_pass http://127.0.0.1:3411;
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Vary "Accept-Encoding";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Admin Next.js images
    location /admin/_next/image {
        proxy_pass http://127.0.0.1:3411;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Admin static assets from public folder (images, etc.)
    # Next.js serves files from public/ at root, but we need them at /admin/
    location /admin/images/ {
        proxy_pass http://127.0.0.1:3411/images/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Admin other static assets from public folder
    location ~ ^/admin/(favicon\.ico|robots\.txt|sitemap\.xml|.*\.(ico|png|jpg|jpeg|gif|svg|webp))$ {
        proxy_pass http://127.0.0.1:3411;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    
    # Admin Next.js other _next paths (images, media, etc.)
    location ~ ^/admin/_next/ {
        proxy_pass http://localhost:3411;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_redirect off;
        proxy_buffering off;
    }

    # Admin dashboard - Next.js app
    # Proxies directly to Admin backend on localhost:3411
    # Access at: http://issue.haahii.com/admin
    # This location must come AFTER /admin/_next/ to allow static assets to be handled first
    location /admin {
        proxy_pass http://localhost:3411;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Next.js specific settings
        proxy_redirect off;
        proxy_buffering off;
    }

    # Frontend - Next.js app
    # Proxies directly to Frontend backend on localhost:3412
    # Access at: http://issue.haahii.com
    location / {
        proxy_pass http://localhost:3412;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Next.js specific settings
        proxy_redirect off;
        proxy_buffering off;
    }

    # Frontend Next.js static assets (_next/static)
    location /_next/static/ {
        proxy_pass http://localhost:3412;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # Health check endpoint
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}

# HTTPS server block (COMMENTED OUT - uncomment after installing SSL certificates)
# To enable HTTPS:
#   1. Install SSL certificates: sudo certbot --nginx -d issue.haahii.com
#   2. Uncomment the server block below (remove # from lines starting with "server {")
#   3. Uncomment the HTTP redirect above (line 18)
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name issue.haahii.com;
#
#     # SSL certificates (Let's Encrypt)
#     # Update paths after running: sudo certbot --nginx -d issue.haahii.com
#     ssl_certificate /etc/letsencrypt/live/issue.haahii.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/issue.haahii.com/privkey.pem;
#
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # Security headers
#     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#
#     client_max_body_size 50M;
#
#     # API endpoints (proxies directly to localhost:3410)
#     location /api/ {
#         limit_req zone=api_limit burst=20 nodelay;
#         proxy_pass http://localhost:3410;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     # Upload endpoints
#     location /api/admin/v1/upload/ {
#         limit_req zone=upload_limit burst=5 nodelay;
#         client_max_body_size 100M;
#         proxy_pass http://localhost:3410;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     # Static files
#     location /uploads/ {
#         alias /usr/share/nginx/html/storage/uploads/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /public/ {
#         alias /usr/share/nginx/html/storage/public/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     # Admin Next.js static assets (_next/static) - MUST come before /admin location
#     location /admin/_next/ {
#         proxy_pass http://localhost:3411;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         add_header Cache-Control "public, max-age=31536000, immutable";
#         proxy_redirect off;
#         proxy_buffering off;
#     }
#
#     # Admin dashboard (proxies directly to localhost:3411)
#     location /admin {
#         proxy_pass http://localhost:3411;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_redirect off;
#         proxy_buffering off;
#     }
#
#     # Frontend (proxies directly to localhost:3412)
#     location / {
#         proxy_pass http://localhost:3412;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /_next/static/ {
#         proxy_pass http://localhost:3412;
#         add_header Cache-Control "public, max-age=31536000, immutable";
#     }
#
#     # Health check
#     location /health {
#         access_log off;
#         return 200 "healthy\n";
#         add_header Content-Type text/plain;
#     }
# }
